const orderByDay = require("../../src/services/order-by-day")

describe("order by day tests", () => {
    it("should group by date", () => {
        const result = orderByDay([
            {
                id,
                
                dataEnvio,
                numeroPedido,
                quantidade,
                tamanho,
                horaExecucao,
                horaTermino
            }
        ])
    })
}) 